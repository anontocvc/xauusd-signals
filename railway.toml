# Railway deployment configuration
# This file tells Railway how to run your app

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python app.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[services]]
name = "xauusd-signals"
